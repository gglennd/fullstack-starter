{
  "name": "@gglennd/main",
  "private": true,
  "scripts": {
    "dev": "pnpm run -r --parallel --aggregate-output dev",
    "lint": "pnpm run -r --parallel lint",
    "prebuild": "pnpm run -r --parallel prebuild",
    "build": "pnpm run -r --parallel build"
  },
  "packageManager": "pnpm@10.6.5"
}